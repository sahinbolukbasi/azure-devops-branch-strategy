# ══════════════════════════════════════════════════════════════════════════════
# Business Central CI/CD Pipeline
# Branch Strategy: feature/* → sandbox → release → main
# ══════════════════════════════════════════════════════════════════════════════
#
# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║                           BRANCH KURALLARI                                 ║
# ╠═══════════════════════════════════════════════════════════════════════════╣
# ║                                                                           ║
# ║  SANDBOX ← feature/* ONLY                                                 ║
# ║     • Sadece feature branch'lerden PR kabul eder                          ║
# ║     • Build YOK (sadece validation)                                       ║
# ║                                                                           ║
# ║  RELEASE ← sandbox, bugfix/* ONLY                                         ║
# ║     • Sandbox veya bugfix branch'lerden PR kabul eder                     ║
# ║     • Merge sonrası BUILD çalışır                                         ║
# ║     • Bugfix merge → Sandbox'a otomatik back-merge                        ║
# ║                                                                           ║
# ║  MAIN ← release, hotfix/* ONLY                                            ║
# ║     • Release veya hotfix branch'lerden PR kabul eder                     ║
# ║     • Merge sonrası BUILD çalışır                                         ║
# ║     • Hotfix merge → Release ve Sandbox'a otomatik back-merge             ║
# ║                                                                           ║
# ╚═══════════════════════════════════════════════════════════════════════════╝
#
# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║                              AKIŞ DİYAGRAMI                                ║
# ╠═══════════════════════════════════════════════════════════════════════════╣
# ║                                                                           ║
# ║  NORMAL GELİŞTİRME:                                                        ║
# ║  feature/* ──PR──→ sandbox ──PR──→ release ──PR──→ main                   ║
# ║              │              │               │                             ║
# ║          Validate       Validate         Validate                         ║
# ║          (no build)     + BUILD          + BUILD                          ║
# ║                                                                           ║
# ║  BUGFIX:                                                                   ║
# ║  bugfix/* ──PR──→ release ──→ BUILD ──→ Back-merge to sandbox            ║
# ║                                                                           ║
# ║  HOTFIX:                                                                  ║
# ║  hotfix/* ──PR──→ main ──→ BUILD ──→ Back-merge to release & sandbox     ║
# ║                                                                           ║
# ╚═══════════════════════════════════════════════════════════════════════════╝
#
# TAG & DEPLOY: 
# - Tag eklenince ayrı pipeline canlıya alır
# ══════════════════════════════════════════════════════════════════════════════

trigger:
  branches: 
    include: 
      - main
      - release
      - sandbox

pr:
  branches: 
    include: 
      - main
      - release
      - sandbox

variables:
  # PR Detection
  isPR: $[eq(variables['Build.Reason'], 'PullRequest')]
  
  # Branch Detection (Merge sonrası)
  isMain: $[eq(variables['Build.SourceBranch'], 'refs/heads/main')]
  isRelease: $[eq(variables['Build.SourceBranch'], 'refs/heads/release')]
  isSandbox:  $[eq(variables['Build.SourceBranch'], 'refs/heads/sandbox')]
  
  # Build Configuration
  buildConfiguration: 'Release'

pool:
  vmImage: windows-latest

# ══════════════════════════════════════════════════════════════════════════════
# STAGES
# ══════════════════════════════════════════════════════════════════════════════

stages: 
  # ════════════════════════════════════════════════════════════════════════════
  # STAGE 1: PR VALIDATION
  # Tüm PR'larda çalışır - Yanlış branch'ten PR'ı ENGELLER
  # ════════════════════════════════════════════════════════════════════════════
  - stage:  Validate
    displayName: "🔍 PR Validation"
    condition: eq(variables.isPR, true)
    jobs: 
      - job: ValidateBranchRules
        displayName: "Branch Rules Check"
        steps: 
          - checkout: none

          - powershell: |
              # ═══════════════════════════════════════════════════════════════
              # Branch Validation Script
              # Yanlış PR'ları engelleyerek geliştiricilerin hata yapmasını önler
              # ═══════════════════════════════════════════════════════════════
              
              $sourceBranch = "$(System.PullRequest.SourceBranch)" -replace "refs/heads/", ""
              $targetBranch = "$(System.PullRequest.TargetBranch)" -replace "refs/heads/", ""
              
              Write-Host ""
              Write-Host "═══════════════════════════════════════��══════════════════════"
              Write-Host "                    🔍 PR VALIDATION                          "
              Write-Host "══════════════════════════════════════════════════════════════"
              Write-Host ""
              Write-Host "  📤 Kaynak Branch :  $sourceBranch"
              Write-Host "  📥 Hedef Branch  : $targetBranch"
              Write-Host ""
              
              $valid = $false
              $willBuildAfterMerge = $false
              $willBackMerge = $false
              $backMergeTarget = ""
              $errorMessage = ""
              
              switch ($targetBranch) {
                  # ──────────────────────────────────────────────────────────
                  # SANDBOX BRANCH RULES
                  # Sadece feature/* kabul edilir
                  # ──────────────────────────────────────────────────────────
                  "sandbox" {
                      if ($sourceBranch -like "feature/*") {
                          $valid = $true
                          $willBuildAfterMerge = $false
                          Write-Host "  ✅ Geçerli:  feature/* → sandbox"
                          Write-Host ""
                          Write-Host "  ℹ️  Bu merge sonrası BUILD yapılmayacak."
                          Write-Host "      Kod sandbox'ta test edilecek."
                      }
                      else {
                          $errorMessage = @"

  ╔═══════════════════════════════════════════════════════════════════╗
  ║             ❌ SANDBOX BRANCH KURALI İHLALİ!                      ║
  ╠═══════════════════════════════════════════════════════════════════╣
  ║                                                                   ║
  ║  Sandbox branch'e SADECE feature/* branch'lerden PR açılabilir!   ║
  ║                                                                   ║
  ║  ✅ İZİN VERİLEN:                                                 ║
  ║     • feature/*  → Yeni özellik geliştirmeleri                    ║
  ║                                                                   ║
  ║  ❌ SİZİN BRANCH:                                                  ║
  ║     • $sourceBranch
  ║                                                                   ║
  ║  ═══════════════════════════════════════════════════════════════  ║
  ║                                                                   ║
  ║  📋 DOĞRU KULLANIM:                                                ║
  ║                                                                   ║
  ║     1.sandbox branch'inden yeni branch açın:                      ║
  ║        git checkout sandbox                                       ║
  ║        git checkout -b feature/yeni-ozellik                       ║
  ║                                                                   ║
  ║     2.Geliştirmenizi yapın ve push edin                          ║
  ║                                                                   ║
  ║     3.feature/yeni-ozellik → sandbox PR açın                     ║
  ║                                                                   ║
  ╚═══════════════════════════════════════════════════════════════════╝
"@
                      }
                  }
                  
                  # ──────────────────────────────────────────────────────────
                  # RELEASE BRANCH RULES
                  # Sadece sandbox veya bugfix/* kabul edilir
                  # ──────────────────────────────────────────────────────────
                  "release" {
                      if ($sourceBranch -eq "sandbox") {
                          $valid = $true
                          $willBuildAfterMerge = $true
                          Write-Host "  ✅ Geçerli: sandbox → release"
                          Write-Host ""
                          Write-Host "  📦 Bu merge sonrası BUILD çalışacak."
                      }
                      elseif ($sourceBranch -like "bugfix/*") {
                          $valid = $true
                          $willBuildAfterMerge = $true
                          $willBackMerge = $true
                          $backMergeTarget = "sandbox"
                          Write-Host "  ✅ Geçerli: bugfix/* → release"
                          Write-Host ""
                          Write-Host "  📦 Bu merge sonrası BUILD çalışacak."
                          Write-Host "  🔄 Otomatik back-merge:  release → sandbox"
                      }
                      else {
                          $errorMessage = @"

  ╔═══════════════════════════════════════════════════════════════════╗
  ║             ❌ RELEASE BRANCH KURALI İHLALİ!                       ║
  ╠═══════════════════════════════════════════════════════════════════╣
  ║                                                                   ║
  ║  Release branch'e SADECE şu kaynaklardan PR açılabilir:            ║
  ║                                                                   ║
  ║  ✅ İZİN VERİLEN:                                                  ║
  ║     • sandbox   → Normal geliştirme akışı                         ║
  ║     • bugfix/*  → Release ortamı hata düzeltmeleri                ║
  ║                                                                   ║
  ║  ❌ SİZİN BRANCH:                                                  ║
  ║     • $sourceBranch
  ║                                                                   ║
  ║  ═══════════════════════════════════════════════════════════════  ║
  ║                                                                   ║
  ║  📋 DOĞRU KULLANIM:                                                ║
  ║                                                                   ║
  ║  Normal Akış:                                                     ║
  ║     feature/* → sandbox → release                                 ║
  ║                                                                   ║
  ║  Bugfix İçin:                                                     ║
  ║     1.release branch'inden bugfix açın:                           ║
  ║        git checkout release                                       ║
  ║        git checkout -b bugfix/hata-aciklamasi                     ║
  ║                                                                   ║
  ║     2.Düzeltmeyi yapın ve push edin                              ║
  ║                                                                   ║
  ║     3.bugfix/hata-aciklamasi → release PR açın                   ║
  ║                                                                   ║
  ╚═══════════════════════════════════════════════════════════════════╝
"@
                      }
                  }
                  
                  # ──────────────────────────────────────────────────────────
                  # MAIN BRANCH RULES
                  # Sadece release veya hotfix/* kabul edilir
                  # ──────────────────────────────────────────────────────────
                  "main" {
                      if ($sourceBranch -eq "release") {
                          $valid = $true
                          $willBuildAfterMerge = $true
                          Write-Host "  ✅ Geçerli: release → main"
                          Write-Host ""
                          Write-Host "  📦 Bu merge sonrası BUILD çalışacak."
                          Write-Host "  🌟 Production-ready kod!"
                      }
                      elseif ($sourceBranch -like "hotfix/*") {
                          $valid = $true
                          $willBuildAfterMerge = $true
                          $willBackMerge = $true
                          $backMergeTarget = "release ve sandbox"
                          Write-Host "  ✅ Geçerli:  hotfix/* → main"
                          Write-Host ""
                          Write-Host "  📦 Bu merge sonrası BUILD çalışacak."
                          Write-Host "  🔄 Otomatik back-merge: main → release → sandbox"
                          Write-Host "  🔥 Acil düzeltme modu!"
                      }
                      else {
                          $errorMessage = @"

  ╔═══════════════════════════════════════════════════════════════════╗
  ║               ❌ MAIN BRANCH KURALI İHLALİ!                        ║
  ╠═══════════════════════════════════════════════════════════════════╣
  ║                                                                   ║
  ║  Main branch'e SADECE şu kaynaklardan PR açılabilir:              ║
  ║                                                                   ║
  ║  ✅ İZİN VERİLEN:                                                  ║
  ║     • release   → Normal release akışı                            ║
  ║     • hotfix/*  → Acil canlı düzeltmeleri                         ║
  ║                                                                   ║
  ║  ❌ SİZİN BRANCH:                                                   ║
  ║     • $sourceBranch
  ║                                                                   ║
  ║  ═══════════════════════════════════════════════════════════════  ║
  ║                                                                   ║
  ║  📋 DOĞRU KULLANIM:                                                ║
  ║                                                                   ║
  ║  Normal Akış:                                                     ║
  ║     feature/* → sandbox → release → main                          ║
  ║                                                                   ║
  ║  Hotfix İçin (ACİL DURUM):                                        ║
  ║     1.main branch'inden hotfix açın:                              ║
  ║        git checkout main                                          ║
  ║        git checkout -b hotfix/acil-duzeltme                       ║
  ║                                                                   ║
  ║     2.Düzeltmeyi yapın ve push edin                              ║
  ║                                                                   ║
  ║     3.hotfix/acil-duzeltme → main PR açın                        ║
  ║                                                                   ║
  ╚═══════════════════════════════════════════════════════════════════╝
"@
                      }
                  }
                  
                  default {
                      $errorMessage = @"

  ╔═══════════════════════════════════════════════════════════════════╗
  ║               ❌ GEÇERSİZ HEDEF BRANCH!                             ║
  ╠═══════════════════════════════════════════════════════════════════╣
  ║                                                                   ║
  ║  '$targetBranch' branch'i için PR kuralı tanımlanmamış.
  ║                                                                   ║
  ║  Sadece şu branch'lere PR açılabilir:                             ║
  ║     • sandbox                                                     ║
  ║     • release                                                     ║
  ║     • main                                                        ║
  ║                                                                   ║
  ╚═══════════════════════════════════════════════════════════════════╝
"@
                  }
              }
              
              # ──────────────────────────────────────────────────────────
              # Sonuç
              # ──────────────────────────────────────────────────────────
              Write-Host ""
              
              if ($valid) {
                  Write-Host "══════════════════════════════════════════════════════════════"
                  Write-Host "  ✅ PR VALIDATION BAŞARILI"
                  Write-Host "══════════════════════════════════════════════════════════════"
                  Write-Host ""
                  Write-Host "  $sourceBranch → $targetBranch"
                  Write-Host ""
                  
                  if ($willBuildAfterMerge) {
                      Write-Host "  📦 Merge sonrası:  BUILD"
                  }
                  if ($willBackMerge) {
                      Write-Host "  🔄 Merge sonrası: Back-merge → $backMergeTarget"
                  }
                  if (-not $willBuildAfterMerge -and -not $willBackMerge) {
                      Write-Host "  ⚡ Merge sonrası: Sadece kod birleştirilecek"
                  }
                  
                  Write-Host ""
                  Write-Host "══════════════════════════════════════════════════════════════"
              } else {
                  Write-Host "##[error]$errorMessage"
                  exit 1
              }
            displayName: "🔍 Validate Branch Rules"
            failOnStderr: true

  # ════════════════════════════════════════════════════════════════════════════
  # STAGE 2: BUILD - RELEASE BRANCH
  # sandbox → release veya bugfix/* → release merge sonrası
  # ════════════════════════════════════════════════════════════════════════════
  - stage: BuildRelease
    displayName: "🔨 Build (Release)"
    condition: |
      and(
        succeeded(),
        ne(variables.isPR, true),
        eq(variables.isRelease, true)
      )
    jobs:
      - job: CompileApp
        displayName:  "Compile AL Application"
        timeoutInMinutes:  60
        steps: 
          - checkout: self
            clean: true
            fetchDepth: 0

          - task: PowerShell@2
            displayName: "📦 Build & Package"
            inputs: 
              targetType: inline
              script: |
                Write-Host ""
                Write-Host "═══════════════════════════════════════════════════════════════"
                Write-Host "                    🎯 RELEASE BUILD                           "
                Write-Host "═══════════════════════════════════════════════════════════════"
                Write-Host ""
                Write-Host "  📌 Branch      : release"
                Write-Host "  📌 Build       : $(Build.BuildNumber)"
                Write-Host "  📌 Commit      : $(Build.SourceVersion)"
                Write-Host "  📌 Kaynak      : sandbox veya bugfix/* merge"
                Write-Host ""
                
                if (Test-Path "$(Build.SourcesDirectory)/CompileApp.ps1") {
                    Write-Host "  ✅ CompileApp.ps1 bulundu, çalıştırılıyor..."
                    Write-Host ""
                    & "$(Build.SourcesDirectory)/CompileApp.ps1"
                } else {
                    Write-Host "  ##[warning]CompileApp.ps1 bulunamadı."
                    Write-Host "  Örnek build işlemi yapılıyor..."
                    Write-Host ""
                    
                    New-Item -ItemType Directory -Force -Path "$(Build.StagingDirectory)" | Out-Null
                    
                    @{
                        BuildNumber = "$(Build.BuildNumber)"
                        Branch = "release"
                        Commit = "$(Build.SourceVersion)"
                        BuildDate = (Get-Date).ToString("yyyy-MM-dd HH: mm:ss")
                        BuildType = "Release"
                    } | ConvertTo-Json | Out-File "$(Build.StagingDirectory)/build-info.json"
                    
                    Write-Host "  ✅ Build bilgisi oluşturuldu."
                }
                
                Write-Host ""
                Write-Host "═══════════════════════════════════════════════════════════════"
                Write-Host "                    ✅ BUILD COMPLETED                         "
                Write-Host "═══════════════════════════════════════════════════════════════"
              workingDirectory: $(Build.SourcesDirectory)

          - publish: $(Build.StagingDirectory)
            artifact: release-$(Build.BuildNumber)
            displayName: "📤 Publish Artifact"
            condition: succeeded()

  # ════════════════════════════════════════════════════════════════════════════
  # STAGE 3: BUILD - MAIN BRANCH
  # release → main veya hotfix/* → main merge sonrası
  # ════════════════════════════════════════════════════════════════════════════
  - stage: BuildMain
    displayName:  "🔨 Build (Main)"
    condition: |
      and(
        succeeded(),
        ne(variables.isPR, true),
        eq(variables.isMain, true)
      )
    jobs:
      - job: CompileApp
        displayName: "Compile AL Application"
        timeoutInMinutes: 60
        steps: 
          - checkout: self
            clean: true
            fetchDepth: 0

          - task: PowerShell@2
            displayName: "📦 Build & Package"
            inputs: 
              targetType:  inline
              script:  |
                Write-Host ""
                Write-Host "═══════════════════════════════════════════════════════════════"
                Write-Host "                    🌟 PRODUCTION BUILD                        "
                Write-Host "═══════════════════════════════════════════════════════════════"
                Write-Host ""
                Write-Host "  📌 Branch      : main"
                Write-Host "  📌 Build       : $(Build.BuildNumber)"
                Write-Host "  📌 Commit      : $(Build.SourceVersion)"
                Write-Host "  📌 Kaynak      : release veya hotfix/* merge"
                Write-Host ""
                
                if (Test-Path "$(Build.SourcesDirectory)/CompileApp.ps1") {
                    Write-Host "  ✅ CompileApp.ps1 bulundu, çalıştırılıyor..."
                    Write-Host ""
                    & "$(Build.SourcesDirectory)/CompileApp.ps1"
                } else {
                    Write-Host "  ##[warning]CompileApp.ps1 bulunamadı."
                    Write-Host "  Örnek build işlemi yapılıyor..."
                    Write-Host ""
                    
                    New-Item -ItemType Directory -Force -Path "$(Build.StagingDirectory)" | Out-Null
                    
                    @{
                        BuildNumber = "$(Build.BuildNumber)"
                        Branch = "main"
                        Commit = "$(Build.SourceVersion)"
                        BuildDate = (Get-Date).ToString("yyyy-MM-dd HH:mm:ss")
                        BuildType = "Production"
                    } | ConvertTo-Json | Out-File "$(Build.StagingDirectory)/build-info.json"
                    
                    Write-Host "  ✅ Build bilgisi oluşturuldu."
                }
                
                Write-Host ""
                Write-Host "═══════════════════════════════════════════════════════════════"
                Write-Host "                    ✅ BUILD COMPLETED                         "
                Write-Host "═══════════════════════════════════════════════════════════════"
              workingDirectory: $(Build.SourcesDirectory)

          - publish: $(Build.StagingDirectory)
            artifact: production-$(Build.BuildNumber)
            displayName: "📤 Publish Artifact"
            condition:  succeeded()

  # ════════════════════════════════════════════════════════════════════════════
  # STAGE 4: BACK-MERGE (Release → Sandbox)
  # Bugfix merge sonrası sandbox'ı güncelle
  # Release branch'e her push'ta çalışır (sandbox veya bugfix merge)
  # ════════════════════════════════════════════════════════════════════════════
  - stage: BackMergeToSandbox
    displayName: "🔄 Sync:  Release → Sandbox"
    dependsOn: BuildRelease
    condition: |
      and(
        succeeded(),
        eq(variables.isRelease, true)
      )
    jobs:
      - job: SyncSandbox
        displayName: "Back-merge to Sandbox"
        steps:
          - checkout: self
            persistCredentials: true
            fetchDepth: 0

          - powershell: |
              Write-Host ""
              Write-Host "══════════════════════════════════════════════════════════════"
              Write-Host "              🔄 RELEASE → SANDBOX SYNC                       "
              Write-Host "══════════════════════════════════════════════════════════════"
              Write-Host ""
              
              git config user.email "devops-bot@yourcompany.com"
              git config user.name "DevOps Bot"
              git fetch origin --prune
              
              Write-Host "  📌 Release'deki değişiklikler sandbox'a aktarılıyor..."
              Write-Host ""
              
              git checkout sandbox 2>&1
              if ($LASTEXITCODE -ne 0) {
                  Write-Host "  ##[error]Sandbox branch'e geçilemedi!"
                  exit 1
              }
              
              git pull origin sandbox --no-edit 2>&1
              
              $mergeResult = git merge origin/release -m "[skip ci] 🔄 Auto-sync:  Release → Sandbox (bugfix/feature merge)" --no-edit 2>&1
              
              if ($LASTEXITCODE -ne 0) {
                  Write-Host ""
                  Write-Host "  ╔═══════════════════════════════════════════════════════════╗"
                  Write-Host "  ║  ⚠️ CONFLICT DETECTED:  Release → Sandbox                  ║"
                  Write-Host "  ╠═══════════════════════════════════════════════════════════╣"
                  Write-Host "  ║                                                           ║"
                  Write-Host "  ║  Manuel çözüm gerekiyor:                                   ║"
                  Write-Host "  ║                                                           ║"
                  Write-Host "  ║  1.git checkout sandbox                                  ║"
                  Write-Host "  ║  2.git pull origin sandbox                               ║"
                  Write-Host "  ║  3.git merge origin/release                              ║"
                  Write-Host "  ║  4. Conflict'leri çözün                                   ║"
                  Write-Host "  ║  5.git add .                                             ║"
                  Write-Host "  ║  6.git commit -m 'Merge release into sandbox'            ║"
                  Write-Host "  ║  7.git push origin sandbox                               ║"
                  Write-Host "  ║                                                           ║"
                  Write-Host "  ╚═══════════════════════════════════════════════════════════╝"
                  Write-Host ""
                  
                  git merge --abort 2>&1
                  Write-Host "  ##[warning]Back-merge iptal edildi. Manuel müdahale gerekli."
              } else {
                  $pushResult = git push origin sandbox 2>&1
                  
                  if ($LASTEXITCODE -eq 0) {
                      Write-Host "  ✅ Release → Sandbox sync başarılı!"
                  } else {
                      Write-Host "  ℹ️ Değişiklik yok veya push başarısız."
                  }
              }
              
              Write-Host ""
              Write-Host "══════════════════════════════════════════════════════════════"
            displayName: "🔄 Execute Back-Merge"

  # ════════════════════════════════════════════════════════════════════════════
  # STAGE 5: BACK-MERGE (Main → Release → Sandbox)
  # Hotfix merge sonrası tüm branch'leri güncelle
  # Main branch'e her push'ta çalışır (release veya hotfix merge)
  # ════════════════════════════════════════════════════════════════════════════
  - stage:  BackMergeFromMain
    displayName:  "🔄 Sync: Main → Release → Sandbox"
    dependsOn: BuildMain
    condition:  |
      and(
        succeeded(),
        eq(variables.isMain, true)
      )
    jobs:
      - job: SyncAllBranches
        displayName: "Back-merge to Release & Sandbox"
        steps:
          - checkout: self
            persistCredentials: true
            fetchDepth: 0

          - powershell:  |
              Write-Host ""
              Write-Host "══════════════════════════════════════════════════════════════"
              Write-Host "              🔄 MAIN → RELEASE → SANDBOX SYNC                "
              Write-Host "══════════════════════════════════════════════════════════════"
              Write-Host ""
              
              git config user.email "devops-bot@yourcompany.com"
              git config user.name "DevOps Bot"
              git fetch origin --prune
              
              $hasError = $false
              
              # ────────────────────────────────────────────────────────────
              # ADIM 1: Main → Release
              # ────────────────────────────────────────────────────────────
              Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              Write-Host "  📌 ADIM 1: Main → Release"
              Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              Write-Host ""
              
              git checkout release 2>&1
              git pull origin release --no-edit 2>&1
              
              $mergeResult = git merge origin/main -m "[skip ci] 🔄 Auto-sync: Main → Release (hotfix/release merge)" --no-edit 2>&1
              
              if ($LASTEXITCODE -ne 0) {
                  Write-Host "  ##[warning]❌ CONFLICT:  Main → Release"
                  Write-Host "  Manuel çözüm gerekiyor!"
                  git merge --abort 2>&1
                  $hasError = $true
              } else {
                  git push origin release 2>&1
                  if ($LASTEXITCODE -eq 0) {
                      Write-Host "  ✅ Main → Release başarılı!"
                  } else {
                      Write-Host "  ℹ️ Değişiklik yok."
                  }
              }
              
              # ────────────────────────────────────────────────────────────
              # ADIM 2: Release → Sandbox
              # ────────────────────────────────────────────────────────────
              Write-Host ""
              Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              Write-Host "  📌 ADIM 2: Release → Sandbox"
              Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              Write-Host ""
              
              git checkout sandbox 2>&1
              git pull origin sandbox --no-edit 2>&1
              
              $mergeResult = git merge origin/release -m "[skip ci] 🔄 Auto-sync: Release → Sandbox (from main)" --no-edit 2>&1
              
              if ($LASTEXITCODE -ne 0) {
                  Write-Host "  ##[warning]❌ CONFLICT:  Release → Sandbox"
                  Write-Host "  Manuel çözüm gerekiyor!"
                  git merge --abort 2>&1
                  $hasError = $true
              } else {
                  git push origin sandbox 2>&1
                  if ($LASTEXITCODE -eq 0) {
                      Write-Host "  ✅ Release → Sandbox başarılı!"
                  } else {
                      Write-Host "  ℹ️ Değişiklik yok."
                  }
              }
              
              # ────────────────────────────────────────────────────────────
              # Sonuç
              # ────────────────────────────────────────────────────────────
              Write-Host ""
              Write-Host "══════════════════════════════════════════════════════════════"
              
              if ($hasError) {
                  Write-Host "  ##[warning]⚠️ BACK-MERGE TAMAMLANDI (CONFLICT'LER VAR)"
                  Write-Host ""
                  Write-Host "  Lütfen conflict'leri manuel olarak çözün."
              } else {
                  Write-Host "  ✅ TÜM BRANCH'LER SENKRONİZE EDİLDİ"
                  Write-Host ""
                  Write-Host "  Main → Release → Sandbox ✓"
              }
              
              Write-Host "══════════════════════════════════════════════════════════════"
            displayName: "🔄 Execute Back-Merge"
